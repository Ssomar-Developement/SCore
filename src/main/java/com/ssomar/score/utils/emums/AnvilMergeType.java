package com.ssomar.score.utils.emums;

public enum AnvilMergeType {
    INPUT1_AS_RESULT,
    INPUT2_AS_RESULT,
    CUSTOM_RESULT,
    // TODO INPUT1_MERGE_INPUT2 ( + with merge options , MERGE_ENCHANTS, MERGE_ATTRIBUTES, ...)
    ;
}
